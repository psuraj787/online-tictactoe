import React from "react";
import Header from "../UI/Header";

const Login = () => {
  return (
    <React.Fragment>
      <Header />
      <div>Login</div>
    </React.Fragment>
  );
};

export default Login;
